name: Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest

    defaults:
      run:
        shell: bash

    steps:
      - run: |
          TMP="$(mktemp -d)"
          git clone https://github.com/rust-lang/rust-clippy "$TMP"
          cd "$TMP"
          cargo uitest
